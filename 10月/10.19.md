## 通过定时器防抖，修复popover慢速移动导致关闭问题
```js
let timer
const onMouseenter = () => {
  isVisible.value = true
  if (timer) {
    clearTimeout(timer)
  }
}

//延迟关闭
const onMouseleave = () => {
  timer = setTimeout(() => {
    isVisible.value = false
    timer = null
  }, DELAY_TIME)
}
```